// Network source tags for DetectedNetwork.source
// Keep this header tiny and dependency-free: shared across core + modes.
#pragma once

#include <stdint.h>

static constexpr uint8_t NET_SOURCE_LOCAL = 0;
static constexpr uint8_t NET_SOURCE_C5    = 1;

